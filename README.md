我有一个django前后端项目，目前web界面通过输入ip，需要扩容的磁盘，扩容的磁盘大小，vc_service会调用vcenter接口，给这台机器把空间加上，再ansible_service会ssh连接ansible所在的ubuntu服务器执行脚本，给机器的文件系统扩容，存在的问题有：
1、前端界面简陋不够美观，
2、增加审批流程，后续会对接内网的SSO，有一个管理员审批其他所有用户的磁盘扩容申请流程
3、用户点击扩容按钮时，前端界面会转圈圈等程序执行过程太长，我想通过把任务记下来的方式让程序后台执行
4、增加管理员调用vcenter接口给机器扩容前，能够根据申请的磁盘判断磁盘所在的vcenter存储空间大小
5、程序能够识别申请扩容的磁盘容量大小，若小于200GB且存储空间满足扩容，可直接扩容无需管理员审批
6、目前若vcenter虚拟机两块磁盘大小相同，程序无法区分，请你给出解决方法
7、如果vcenter的虚拟机处于关机状态，帮我把虚拟机磁盘空间增加后，输出消息需将虚拟机开机手动扩容
8、如果vcenter的虚拟机存在快照无法扩容，输出消息提醒存在快照，无法在原磁盘扩容
